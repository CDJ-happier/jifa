# Eclipse Jifa 128GB大文件上传配置说明

## 📋 配置修改总结

已完成的配置修改包括以下三个方面，确保支持128GB大文件上传：

### 1. Spring Multipart配置 (application.yml)
```yaml
spring:
  servlet:
    multipart:
      max-request-size: 128GB    # 修改前: 512MB
      max-file-size: 128GB       # 修改前: 512MB
```

### 2. Tomcat连接器配置 (HttpConfigurer.java)
```java
@Bean
TomcatServletWebServerFactory tomcatServletWebServerFactory() {
    TomcatServletWebServerFactory tomcatServletWebServerFactory = new TomcatServletWebServerFactory();
    tomcatServletWebServerFactory.addConnectorCustomizers(connector -> {
        connector.setAsyncTimeout(-1);
        // 设置最大POST大小以支持128GB文件上传
        connector.setMaxPostSize(137438953472L); // 128GB in bytes
        connector.setMaxSwallowSize(137438953472L); // 128GB in bytes
    });
    return tomcatServletWebServerFactory;
}
```

### 3. 存储路径配置 (application.yml)
```yaml
jifa:
  storage-path: /data/jifa-storage  # 建议使用数据盘而非系统盘
```

## 🔧 技术实现细节

### 文件上传流程
1. **前端上传** → Vue组件通过Element Plus的ElUpload组件上传
2. **Spring MVC接收** → 通过MultipartFile接收文件
3. **Tomcat处理** → Tomcat连接器处理大文件上传
4. **Jifa处理** → FileService.handleUploadRequest()方法处理文件存储

### 关键配置参数说明

| 配置项 | 作用 | 默认值 | 修改后值 |
|--------|------|--------|----------|
| `max-file-size` | 单个文件最大大小 | 512MB | 128GB |
| `max-request-size` | 整个请求最大大小 | 512MB | 128GB |
| `maxPostSize` | Tomcat POST请求最大大小 | 2MB | 128GB |
| `maxSwallowSize` | Tomcat吞入数据最大大小 | 2MB | 128GB |

## ⚡ 性能优化建议

### 服务器配置要求
- **内存**: 至少64GB RAM（推荐128GB+）
- **磁盘**: 高速SSD，建议NVMe SSD
- **网络**: 千兆或万兆网络
- **CPU**: 多核心处理器

### JVM参数优化
```bash
# 针对大文件上传的JVM优化
-Xmx64g -Xms32g                    # 堆内存设置
-XX:MaxMetaspaceSize=2g            # 元空间大小
-XX:+UseG1GC                       # 使用G1垃圾回收器
-XX:MaxGCPauseMillis=200           # 最大GC暂停时间
-XX:ParallelGCThreads=8            # 并行GC线程数
```

## 🚀 部署验证

### 1. 配置验证
重启服务后，可以通过以下方式验证配置是否生效：

```bash
# 检查Spring Boot启动日志
cat logs/application.log | grep "multipart"

# 检查Tomcat配置
cat logs/application.log | grep "Tomcat"
```

### 2. 功能测试
使用curl命令测试大文件上传：

```bash
# 创建测试文件（1GB）
dd if=/dev/zero of=test_file.bin bs=1M count=1024

# 测试上传
curl -X POST \
  -F "file=@test_file.bin" \
  -H "Authorization: Bearer <token>" \
  http://localhost:8102/jifa-api/files/upload
```

## ⚠️ 注意事项

### 1. 内存使用
- 大文件上传会占用大量内存，确保服务器有足够的内存
- 考虑使用流式处理减少内存占用

### 2. 超时设置
- 大文件上传需要较长时间，调整客户端和服务器的超时设置
- 建议设置30分钟以上的超时时间

### 3. 磁盘空间
- 确保数据盘有足够的空间存储大文件
- 建议设置自动清理机制，避免磁盘满

### 4. 网络带宽
- 大文件上传对网络带宽要求较高
- 考虑使用压缩或分片上传优化网络传输

## 🔒 安全考虑

### 1. 文件类型限制
```java
// 在文件上传服务中添加文件类型验证
public long handleUploadRequest(FileType type, MultipartFile file) throws Throwable {
    // 验证文件类型和大小
    Validate.isTrue(file.getSize() <= MAX_FILE_SIZE, "文件过大");
    Validate.isTrue(isAllowedFileType(file.getContentType()), "不支持的文件类型");
    // ...
}
```

### 2. 访问控制
- 确保只有授权用户才能上传大文件
- 实施速率限制，防止滥用

## 📊 监控和日志

### 1. 监控指标
- 文件上传成功率
- 上传平均时间
- 内存使用情况
- 磁盘空间使用率

### 2. 日志配置
```yaml
logging:
  level:
    org.eclipse.jifa.server.service.impl.FileServiceImpl: DEBUG
    org.springframework.web.multipart: DEBUG
```

## 🛠️ 故障排除

### 常见问题及解决方案

1. **上传失败：文件过大**
   - 检查所有配置项是否都已正确设置
   - 验证Tomcat和Spring配置是否生效

2. **内存溢出**
   - 增加JVM堆内存
   - 优化垃圾回收策略

3. **上传超时**
   - 调整客户端和服务器的超时设置
   - 检查网络连接稳定性

## 📞 技术支持

如果遇到问题，请检查：
1. 所有配置文件是否正确修改
2. 服务器资源是否充足
3. 日志文件中是否有错误信息
4. 网络连接是否正常

通过以上配置，Eclipse Jifa现在可以支持最大128GB的文件上传，满足大dump文件的分析需求。